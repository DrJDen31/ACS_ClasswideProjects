# Experiment 10: ANN-in-SSD design space exploration (synthetic 100k)

NUM_BASE=100000
NUM_QUERIES=2000
DIM=128
K=10
SEED=42
DATASET_NAME="synthetic_gaussian"

# ANN hardware level and mode
ANN_HW_LEVEL="L2"
ANN_MODE="cheated"

# Design-space sweeps
# Vectors per block (K), max_steps (0 meaning full scan), and portal_degree.
# For NUM_BASE=100k we narrow to a single Kpb/portal and use a denser max_steps sweep
# to probe a range of recalls (roughly from ~0.5 up to ~0.99).
K_VALUES="128"
STEPS_VALUES="16 24 32 48 64 80 96 112 128 160 192 224 256 320 384 448 512 640 672 704 736 0"
PORTAL_VALUES="2"

JSON_OUT_BASENAME="exp10"
