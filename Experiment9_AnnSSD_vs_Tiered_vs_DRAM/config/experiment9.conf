# Experiment 9: DRAM vs Tiered vs ANN-SSD (common dataset config)

# Core benchmark parameters
NUM_BASE=20000
NUM_QUERIES=2000
DIM=128
K=10
EF_SEARCH=100
M=16
EF_CONSTRUCTION=200
SEED=42
CACHE_CAPACITY=10000      # used only when MODE="tiered"

# Dataset configuration
# Leave DATASET_PATH empty to use a synthetic Gaussian dataset.
# To switch to SIFT20k, set these paths appropriately and optionally
# adjust NUM_BASE/NUM_QUERIES.
DATASET_PATH=""          # e.g., data/SIFT1M/sift/sift_base.fvecs
DATASET_NAME="synthetic_gaussian"
QUERY_PATH=""            # e.g., data/SIFT1M/sift/sift_query.fvecs
GROUNDTRUTH_PATH=""      # e.g., data/SIFT1M/sift/sift_groundtruth.ivecs

# Output basename (results will be written under results/raw/)
JSON_OUT_BASENAME="exp9"
